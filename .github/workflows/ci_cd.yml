name: CI_CD

on:
  pull_request:
    branches: [master]

jobs:
  build-workflow:
    runs-on: ubuntu-18.04
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: show project struture
        run: ls -la
